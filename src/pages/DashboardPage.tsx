// This file is now redundant - Dashboard.tsx is used directly
// Keeping this as a simple redirect to prevent any remaining import issues
import Dashboard from '@/pages/Dashboard';

const DashboardPage = () => {
  return <Dashboard />;
};

export default DashboardPage;
