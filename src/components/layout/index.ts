/**
 * Layout components for app structure
 */

// Layout components temporarily commented out until components are properly organized
// export { default as AppLayout } from './AppLayout';
// export { default as Header } from './Header';

// Layout-specific types
export type { LayoutProps, HeaderProps, SidebarProps } from './types';